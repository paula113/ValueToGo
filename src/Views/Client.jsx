import React from 'react';
import ClientTable from '../Components/ClientTable';

function Client() {
  // sendCCI(data);
  return (
    <main>
      <h1>socio</h1>
      <ClientTable />
    </main>
  );
}

export default Client;
